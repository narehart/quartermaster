{
  "_comment": "OPTIONAL. Copy to ~/.claude/tokenwise/mcp-policy.json to override the auto-classifier. Everything works with NO policy file; this is only for tuning.",
  "servers": {
    "some-server": "read",
    "another-server": "skip"
  },
  "tools": {
    "mcp__some-server__risky_thing": "write",
    "mcp__some-server__cheap_lookup": "read"
  },
  "tiers": { "read": "scout", "write": "mechanic" }
}
